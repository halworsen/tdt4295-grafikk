package vga

import chisel3._
import chisel3.tester._
import org.scalatest.FreeSpec

class ClockSpec extends FreeSpec with ChiselScalatestTester {
  "Should initialize clock at 25MHz" in {
    println("Success!")
  }
}
